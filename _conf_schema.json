{
  "server": {
    "description": "房间服务",
    "type": "object",
    "hint": "通话与观影房间使用独立顶层页面，避免 AstrBot 拓展页沙箱阻止麦克风。默认只允许本机访问。",
    "items": {
      "enabled": {
        "description": "启用房间服务",
        "type": "bool",
        "default": true
      },
      "host": {
        "description": "监听地址",
        "type": "string",
        "default": "127.0.0.1",
        "hint": "默认仅本机。跨设备访问时可改为 0.0.0.0，但必须同时配置 HTTPS/WSS 反向代理。",
        "condition": {
          "enabled": true
        }
      },
      "port": {
        "description": "监听端口",
        "type": "int",
        "default": 6321,
        "hint": "端口被占用时会自动尝试后续端口。",
        "condition": {
          "enabled": true
        }
      },
      "public_base_url": {
        "description": "外部访问地址",
        "type": "string",
        "default": "",
        "hint": "跨设备时填写反向代理后的 HTTPS 地址，例如 https://together.example.com。留空时使用本机地址。",
        "condition": {
          "enabled": true
        }
      },
      "ticket_ttl_minutes": {
        "description": "房间链接有效分钟数",
        "type": "int",
        "default": 10,
        "hint": "链接只用于建立当前房间连接，过期后需从拓展页重新打开。",
        "condition": {
          "enabled": true
        }
      }
    }
  },
  "conversation": {
    "description": "实时对话",
    "type": "object",
    "items": {
      "primary_user_id": {
        "description": "主要用户 ID",
        "type": "string",
        "default": "",
        "hint": "留空时尝试从“我会永远陪着你”的主要目标用户中自动识别。"
      },
      "bot_qq_id": {
        "description": "Bot QQ 号覆盖",
        "type": "string",
        "default": "",
        "hint": "通常留空，由“我会永远陪着你”自动识别。多 Bot 环境无法唯一判断时，可填写实际 Bot QQ 号以显示正确头像。"
      },
      "persona_id": {
        "description": "通话人格",
        "type": "string",
        "default": "",
        "_special": "select_persona",
        "hint": "留空时使用 AstrBot 当前默认人格。"
      },
      "chat_provider_id": {
        "description": "通话与观影对话模型",
        "type": "string",
        "default": "",
        "required": true,
        "_special": "select_provider",
        "hint": "必选。负责实时通话、观影对话和人格上下文；支持图片时会直接读取视频帧。"
      },
      "vision_provider_id": {
        "description": "视觉模型",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "hint": "可选。对话模型支持图片时无需配置；对话模型没有视觉能力时，可选择独立视觉模型转述观影或视频通话画面，再由对话模型结合人格与上下文回复。留空还会尝试 AstrBot 默认图片描述模型。"
      },
      "history_turns": {
        "description": "房间上下文轮数",
        "type": "int",
        "default": 12,
        "hint": "仅保留当前房间最近的对话，不保存原始音频。"
      },
      "enable_memory_context": {
        "description": "接入长期记忆",
        "type": "bool",
        "default": true,
        "hint": "每轮按当前话题从“我会牢牢记住你”读取少量相关记忆；没有安装记忆插件时自动跳过。"
      },
      "call_proactive_enabled": {
        "description": "通话安静时主动找话题",
        "type": "bool",
        "default": true,
        "hint": "语音通话持续安静一段时间后，由当前人格判断是否自然开口；用户正在说话、Bot 正在回应或没有合适话题时不会打扰。"
      },
      "model_hangup_enabled": {
        "description": "允许模型自然结束通话",
        "type": "bool",
        "default": true,
        "hint": "模型可结合人格、关系与对话是否自然收束，自主在一句告别播放完成后结束当前语音连接；连续多次静默且用户始终没有新输入时，也可判断用户可能已经离开并自然收尾。不会按固定倒计时强制挂断。"
      },
      "call_idle_seconds": {
        "description": "通话静默判断间隔秒数",
        "type": "int",
        "default": 120,
        "hint": "从用户或 Bot 最后一次说话结束后开始计算，范围 60 到 900 秒；连续静默判断会累计，用户产生新输入后重置。"
      },
      "sync_astrbot_conversation": {
        "description": "同步到 AstrBot 对话记录",
        "type": "bool",
        "default": true,
        "hint": "把网页中完成的用户文字和 Bot 最终回复追加到该用户当前的 AstrBot 私聊对话；没有现有对话时自动创建“一起房间”对话。"
      },
      "record_shared_experiences": {
        "description": "记录有意义的共同经历",
        "type": "bool",
        "default": true,
        "hint": "房间结束时由模型筛选并只保存一条有意义的通话或观影摘要；不会保存原始音频、视频帧或逐句字幕。没有安装记忆插件时自动跳过。"
      },
      "record_visible_turns": {
        "description": "记录房间文字转写",
        "type": "bool",
        "default": false,
        "hint": "默认关闭。开启后把识别出的用户文字和 Bot 最终回复写入记忆时间线，仍不会保存原始音频或视频帧。"
      },
      "system_prompt": {
        "description": "共处补充提示词",
        "type": "text",
        "default": "",
        "editor_mode": true,
        "editor_language": "text",
        "hint": "附加在当前人格后。自然度优先通过这里和内置场景提示控制。"
      }
    }
  },
  "speech": {
    "description": "语音链路",
    "type": "object",
    "hint": "没有 AstrBot STT 时可直接使用 Edge/Chrome 的浏览器语音识别。",
    "items": {
      "stt_mode": {
        "description": "默认语音识别方式",
        "type": "string",
        "default": "auto",
        "options": [
          "auto",
          "browser",
          "astrbot"
        ],
        "hint": "自动模式优先使用浏览器免配置识别，不支持时回退 AstrBot STT。房间内可以临时切换。"
      },
      "stt_provider_id": {
        "description": "AstrBot STT Provider",
        "type": "string",
        "default": "",
        "_special": "select_provider_stt",
        "hint": "可选。留空时跟随 AstrBot 当前 STT；自动模式也会在浏览器识别不可用时使用它。"
      },
      "stt_correction_enabled": {
        "description": "语音转写智能校对",
        "type": "bool",
        "default": true,
        "hint": "优先校正 Bot 名称的近音转写；普通句子会直接进入回复，避免额外等待。不确定时保留原文，不使用生硬的全局替换。"
      },
      "browser_language": {
        "description": "浏览器识别语言",
        "type": "string",
        "default": "zh-CN",
        "hint": "例如 zh-CN、ja-JP、en-US。"
      },
      "tts_provider_id": {
        "description": "TTS Provider",
        "type": "string",
        "default": "",
        "_special": "select_provider_tts",
        "hint": "留空时跟随 AstrBot 当前 TTS。"
      },
      "browser_tts_fallback": {
        "description": "浏览器朗读兜底",
        "type": "bool",
        "default": true,
        "hint": "没有可用 TTS 或服务端合成失败时，使用浏览器 SpeechSynthesis 朗读。"
      },
      "direct_multilingual_tts": {
        "description": "通话外语语音直出",
        "type": "bool",
        "default": true,
        "hint": "通话接通且陪伴插件目标语种为日语或英语时，由主对话模型同时生成目标语种口语和可见正文，优先跳过额外翻译模型；格式不兼容时自动回退原有转换链路。"
      },
      "tts_timeout_seconds": {
        "description": "TTS 最长等待时间（秒）",
        "type": "int",
        "default": 60,
        "range": {
          "min": 15,
          "max": 180,
          "step": 5
        },
        "hint": "主模型未直出目标语种时包含陪伴插件语种转换，之后等待语音合成；超时后立即回退文字或浏览器朗读。"
      },
      "tts_volume_percent": {
        "description": "TTS 播放音量百分比",
        "type": "int",
        "default": 100,
        "range": {
          "min": 0,
          "max": 100,
          "step": 5
        },
        "hint": "控制房间内 Bot 语音的默认播放音量，不影响影片原声音量或 TTS 合成内容；0 表示静音。"
      },
      "realtime_duplex_enabled": {
        "description": "实时流式语音与全双工（实验性）",
        "type": "bool",
        "default": false,
        "hint": "为分句流式 TTS、持续监听和语音插话启用统一实验开关。当前版本先建立配置入口，关闭时保持原有稳定流程。"
      },
      "cosyvoice_enabled": {
        "description": "启用 CosyVoice 兜底联动（默认开启）",
        "type": "bool",
        "default": true,
        "hint": "默认开启：当未配置 tts_provider_id 且 AstrBot 没有在用的人声 TTS 时，自动复用已安装并激活的 astrbot_plugin_cosyvoice 作为最后兜底（不影响已配置的原生/其它 TTS，它们优先级更高）。关闭后完全禁用 CosyVoice，恢复到原有 TTS 链路。"
      },
      "cosyvoice_voice": {
        "description": "CosyVoice 默认音色",
        "type": "string",
        "default": "",
        "hint": "启用 CosyVoice 兜底联动后，此处可手动填写 CosyVoice 插件中已配置的音色名称（如 中文女、中文男 等）；留空即使用 CosyVoice 插件自身配置的默认音色。"
      }
    }
  },
  "watch": {
    "description": "共同观影",
    "type": "object",
    "items": {
      "prepare_knowledge": {
        "description": "观前无剧透准备",
        "type": "bool",
        "default": true,
        "hint": "异步整理视频页公开标题、UP 主、分区、标签和简介，并标注来源范围；不会预读字幕或搜索分集剧情。"
      },
      "auto_comment": {
        "description": "默认开启自然评论",
        "type": "bool",
        "default": true,
        "hint": "在开场、明显镜头变化、普通观察和结尾时让模型判断是否自然开口，允许长时间保持安静。"
      },
      "comment_interval_seconds": {
        "description": "最长观察间隔",
        "type": "int",
        "default": 60,
        "hint": "画面没有明显变化时的兜底观察间隔，不代表每次都会说话。",
        "condition": {
          "auto_comment": true
        }
      },
      "scene_min_interval_seconds": {
        "description": "镜头观察最小间隔",
        "type": "int",
        "default": 18,
        "hint": "浏览器检测到明显镜头变化后，至少间隔多久才提交下一次观察；较小会更及时，也会增加视觉模型调用。",
        "condition": {
          "auto_comment": true
        }
      },
      "memory_refresh_seconds": {
        "description": "临时剧情整理间隔",
        "type": "int",
        "default": 240,
        "hint": "按已播放内容和证据来源整理剧情、人物关系、待确认线索与双方反应，不会读取后续剧情。临时笔记本身不直接写入长期记忆；房间结束后的单条共同经历摘要由“记录有意义的共同经历”控制。",
        "condition": {
          "auto_comment": true
        }
      },
      "duck_video_volume": {
        "description": "Bot 说话时降低原声",
        "type": "bool",
        "default": true,
        "hint": "播放 Bot 语音时平滑降低影片音量，结束后恢复。"
      },
      "duck_volume_percent": {
        "description": "说话时原声音量百分比",
        "type": "int",
        "default": 28,
        "hint": "例如 28 表示暂时降到原音量的 28%。",
        "condition": {
          "duck_video_volume": true
        }
      }
    }
  }
}
